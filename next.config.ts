// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['t4.ftcdn.net', 't3.ftcdn.net', 'res.cloudinary.com'], // Add any other image domains you're using here
  },
};

module.exports = nextConfig;
